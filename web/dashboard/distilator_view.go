package dashboard

// distilatorView lists pending Distilator proposals, shows diffs, and lets a
// human approve or reject — the one write action the dashboard performs
// anywhere. Approval promotes changes through the daemon's SafeWrite path.
//
// Not yet implemented — build sequence step 6 (docs/architecture.md).
func (s *Server) registerDistilatorView() {}
