package dashboard

// memoryView lists projects, browses a project's memory paths and version
// history (ListVersions), and shows a specific version's content decrypted
// server-side — never exposing raw ciphertext or keys to the browser.
//
// Not yet implemented — build sequence step 6 (docs/architecture.md).
func (s *Server) registerMemoryView() {}
