// Command siloctl is the admin CLI: project onboarding (one automated command)
// and the deliberately-manual, per-layer teardown flow.
package main

import "fmt"

func main() {
	fmt.Println("siloctl: not yet implemented — onboarding is build sequence step 2, teardown step 7 (docs/architecture.md)")
}
