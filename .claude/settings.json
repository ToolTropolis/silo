{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "echo \"$(date) hook fired\" >> /tmp/claude-hook-check.txt; gitleaks git --staged --redact --no-banner || { echo 'BLOCKED: gitleaks found a secret in the staged changes. Unstage it and remove the secret; do not use --no-verify.' >&2; exit 2; }",
            "statusMessage": "Scanning staged changes for secrets...",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
